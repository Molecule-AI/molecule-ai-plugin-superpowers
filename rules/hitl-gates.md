# superpowers — HITL Gate Rules

The superpowers plugin is a meta-plugin; HITL gate rules are defined in the
sub-plugins it bundles. Specifically:

- `molecule-hitl` sub-plugin: see `rules/hitl-gates.md` in that plugin's repo

This file is a placeholder. Do not add rules here unless the superpowers plugin
introduces a new HITL capability not present in its sub-plugins.
